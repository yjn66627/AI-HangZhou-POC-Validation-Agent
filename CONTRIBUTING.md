# 协作指南｜CONTRIBUTING

欢迎加入 **AI POC验证与技术决策智能体** 项目。

---

## 一、先读这个

在提交任何内容之前，请先阅读 [`docs/PUBLICATION_POLICY.md`](docs/PUBLICATION_POLICY.md)。
**本仓库是公开仓库，任何提交都会立刻对全世界可见，且无法真正撤回。**

---

## 二、工作流

```
1. 从 main 拉取最新代码
2. 创建功能分支
3. 提交改动
4. 推送分支
5. 创建 Pull Request
6. 等待 Review
7. 合入 main
```

### 分支命名

| 前缀 | 用途 | 示例 |
| --- | --- | --- |
| `feat/` | 新功能 | `feat/ui-dashboard` |
| `fix/` | 修复 | `fix/kb-node-empty` |
| `docs/` | 文档 | `docs/update-progress` |
| `case/` | Case 相关 | `case/test-p2-draft` |

---

## 三、提交信息

说明"改了什么、为什么"，不要只写"update"。

```
docs: 补充 TEST-R3 的判定标准

说明知识库路径读取为何必须先于结论输出。
```

---

## 四、Pull Request

提交 PR 时请说明：

1. 这个 PR 做了什么
2. 为什么需要这个改动
3. 如何验证
4. 是否涉及公开边界（是否含凭据、内部证据、本机路径）

涉及架构、结论口径、公开边界的改动，请打上 `needs-review` 标签。

---

## 五、Issue

创建 Issue 时请选择合适的标签：

| 标签 | 用途 |
| --- | --- |
| `bug` | 功能缺陷 |
| `case` | Case 设计、补充、修订 |
| `ux` | 产品体验问题 |
| `demo` | 演示相关问题 |
| `docs` | 文档问题 |
| `backend` | 后端链路问题 |
| `tool-runtime` | 工具调用与运行时问题 |
| `needs-review` | 需要评审 |

### Bug 报告最低要求

- 复现步骤（可被他人照着复现）
- 预期结果 vs 实际结果
- 涉及版本或阶段

不接受"好像有问题""跑不起来"这类无法复现的报告。

---

## 六、结论表述纪律

这是本项目最容易出错的地方：

| 允许 | 禁止 |
| --- | --- |
| 集成已证明 | 可以上生产 |
| 质量已达标 | 生产可用 |
| 支持受控试运行 | 成本可控 |
| 主链路已跑通 | 稳定性已验证 |

**任何人不得越过 Decision Engine 的输出档位做表述**，包括 README、PR 描述、Issue 评论、演示材料。

---

## 七、不要做的事

- 不要直接向 `main` 推送（除非是初始化提交或紧急修复）
- 不要提交凭据、环境变量文件、内部证据
- 不要提交本机绝对路径
- 不要提交运行产物、缓存、日志
- 不要在 Issue 中粘贴敏感内容
- 不要提交大体积二进制文件而不说明用途

---

## 八、权限说明

团队成员通过仓库 **Collaborator** 邀请加入，拥有：

- Clone / Pull / Push
- 创建分支、提交
- 创建与管理 Issue
- 创建、Review、Merge Pull Request
- 管理 Release

成员权限**仅限本仓库**，不涉及账号控制权、其他仓库、本地文件与个人设置。
**不要共享账号口令，也不要使用他人账号登录。**

---

_有问题就在 Issue 里问，不要猜。_
